import ManagerPath from '@/constants/ManagerPath '
import ResourceURL from '@/constants/ResourceURL '

export default class StockQuantityHistoryConfigs {
    static readonly managerPath = ManagerPath.STOCK_QUANTITY_HISTORY
    static readonly resourceUrl = ResourceURL.STOCK_QUANTITY_HISTORY
    static readonly resourceKey = 'stock quantity history'
    static readonly createTitle = 'Create Stock Quantity History'
    static readonly updateTitle = 'Update Stock Quantity History'
    static readonly searchTitle = 'Search'
}
